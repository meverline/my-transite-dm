apply plugin: "war"

bootWar {
    mainClass = "org.dm.transit.compute.ComputeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
